# Code Flow: Submit Button Click → Response

This document traces the complete code flow when a user clicks the "Submit" button in `checkvote.html`.

## 📋 Complete Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. FRONTEND: checkvote.html (Lines 73-101)                     │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ User clicks "Submit" button                  │
    │ Event listener: submitBtn.addEventListener   │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Collect form data into payload object:         │
    │ - seal (hex string)                            │
    │ - journal (hex string)                         │
    │ - journal_abi (hex string)                    │
    │ - image_id (hex string)                       │
    │ - nullifier, age, is_student, poll_id, etc.    │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ POST request to http://localhost:8080/checkvote│
    │ Headers: Content-Type: application/json        │
    │ Body: JSON.stringify(payload)                  │
    └───────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. BACKEND: app.py - FastAPI Endpoint (Lines 61-88)              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ @app.post("/checkvote")                       │
    │ async def checkvote_endpoint(vote_request)     │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Pydantic validates VoteRequest:              │
    │ - seal: str                                   │
    │ - journal: str                                │
    │ - journal_abi: str                            │
    │ - image_id: str                               │
    │ - nullifier: str                              │
    │ - age: int                                    │
    │ - is_student: bool                            │
    │ - poll_id: int                                │
    │ - option_a: int                               │
    │ - option_b: int                               │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Convert to VoteRequestModel (dataclass)       │
    │ Call: check_vote(vote_model)                  │
    └───────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. UTILS: utils/util.py - check_vote() (Lines 83-135)          │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 3.1: Decode Image ID                     │
    │ - bytes.fromhex(vote.image_id)               │
    │ - Validate length == 32 bytes                │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 3.2: Decode Journal                      │
    │ - bytes.fromhex(vote.journal)                │
    │ - Calculate SHA-256: journal_digest            │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 3.3: Calculate Claim Digest              │
    │ - Call: calculate_claim_digest(              │
    │           image_id, journal_digest)           │
    └───────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. RISC0: risc0/risc0.py - calculate_claim_digest()           │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 4.1: Create Output struct                │
    │ - journal_digest                              │
    │ - assumptions_digest (empty)                  │
    │ - Calculate: output_digest()                  │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 4.2: Create ReceiptClaim                │
    │ - pre_state_digest = image_id                 │
    │ - post_state_digest = SYSTEM_STATE_ZERO       │
    │ - exit_code = HALTED                          │
    │ - output = output_digest                       │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 4.3: Calculate ReceiptClaim Digest      │
    │ - Hash with tag "risc0.ReceiptClaim"          │
    │ - Include: input, pre_state, post_state,      │
    │            output, exit_code                  │
    │ - Return: claim_digest (32 bytes)             │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Return to: utils/util.py                      │
    │ claim_digest = calculate_claim_digest(...)   │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 3.4: Decode Seal                         │
    │ - bytes.fromhex(vote.seal)                    │
    │ - Extract selector (first 4 bytes)             │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 3.5: Get Verifier Parameters            │
    │ - Call: get_verifier_parameters2(selector)   │
    │ - Lookup RISC0 verifier params by selector   │
    └───────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. GROTH16: groth16/parameters.py                              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ - Calculate selector from params              │
    │ - Lookup in _risc0_selector_verifier_        │
    │   parameters map                              │
    │ - Return: VerifierParameters                 │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 3.6: Verify Integrity                    │
    │ - proof_seal = seal_bytes[4:] (skip selector) │
    │ - Call: verify_integrity(                    │
    │           params, proof_seal, claim_digest)   │
    └───────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. GROTH16: groth16/verifier.py - verify_integrity()           │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 6.1: Decode Seal                         │
    │ - Call: decode_seal(seal)                     │
    │ - Extract G1 points: A (64 bytes), C (64)     │
    │ - Extract G2 point: B (128 bytes)            │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 6.2: Split Digests                      │
    │ - control0, control1 = split_digest(        │
    │     params.control_root)                      │
    │ - claim0, claim1 = split_digest(             │
    │     claim_digest)                              │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 6.3: Prepare Public Signals              │
    │ pub_signals = [                               │
    │   control0 (as int),                          │
    │   control1 (as int),                          │
    │   claim0 (as int),                            │
    │   claim1 (as int),                            │
    │   bn254_control_id (reversed, as int)         │
    │ ]                                              │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 6.4: Verify Groth16 Proof                │
    │ - Call: verify_groth16(_vk, proof,           │
    │                        pub_signals)           │
    └───────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ 7. GROTH16: groth16/verifier.py - verify_groth16()            │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 7.1: Compute vkX                        │
    │ - Start with zero point                      │
    │ - For each input:                             │
    │   * Check input < curve_order                 │
    │   * vkX += IC[i+1] * inputs[i]                │
    │ - vkX += IC[0]                                │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 7.2: Prepare Pairing Points             │
    │ G1: [A, -Alpha, -vkX, -C]                    │
    │ G2: [B, Beta, Gamma, Delta]                  │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 7.3: Perform Pairing Check               │
    │ result = e(A, B) * e(-Alpha, Beta) *         │
    │          e(-vkX, Gamma) * e(-C, Delta)       │
    │ - Check: result == FQ12.one() (identity)     │
    │ - If not equal → raise ValueError             │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Return to: utils/util.py                      │
    │ Verification successful!                       │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Step 3.7: Decode Journal ABI                  │
    │ - bytes.fromhex(vote.journal_abi)             │
    │ - Call: decode_bincode_vote(data)             │
    └───────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ 8. UTILS: utils/util.py - decode_bincode_vote()               │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Decode bincode format (little-endian):        │
    │ - u64: string length                         │
    │ - string: nullifier                          │
    │ - u32: age                                   │
    │ - u8: is_student                             │
    │ - u64: poll_id                               │
    │ - u64: option_a                              │
    │ - u64: option_b                              │
    │ - Return: VoteResponse                        │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Return to: app.py                              │
    │ result = check_vote(vote_model)               │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Convert VoteResponse to dict:                 │
    │ {                                              │
    │   "nullifier": ...,                           │
    │   "age": ...,                                 │
    │   "is_student": ...,                          │
    │   "poll_id": ...,                             │
    │   "option_a": ...,                            │
    │   "option_b": ...                             │
    │ }                                              │
    └───────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Return JSON response:                         │
    │ {                                              │
    │   "status": "success",                        │
    │   "result": { ... }                           │
    │ }                                              │
    └───────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ 9. FRONTEND: checkvote.html (Lines 96-97)                      │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────┐
    │ Display response in <pre id="output">         │
    │ - JSON.stringify(data, null, 2)              │
    │ - Pretty formatted JSON                      │
    └───────────────────────────────────────────────┘
```

## 🔑 Key Functions Called

1. **Frontend (JavaScript)**
   - `submitBtn.addEventListener('click', ...)` - Event handler
   - `fetch('http://localhost:8080/checkvote', ...)` - HTTP POST request

2. **Backend (Python)**
   - `checkvote_endpoint()` - FastAPI endpoint handler
   - `check_vote()` - Main vote verification function
   - `calculate_claim_digest()` - RISC0 claim digest calculation
   - `verify_integrity()` - Groth16 proof verification
   - `verify_groth16()` - Core Groth16 pairing check
   - `decode_bincode_vote()` - Decode vote data from bincode format

## 🔐 Security Checks

1. **Input Validation**
   - Pydantic validates all request fields
   - Image ID must be exactly 32 bytes
   - All hex strings are validated

2. **Cryptographic Verification**
   - SHA-256 hash of journal
   - RISC0 claim digest calculation
   - Groth16 zkSNARK proof verification using BN128 pairing
   - Verifier parameters lookup by selector

3. **Error Handling**
   - Try-catch blocks at each level
   - HTTP 500 errors for verification failures
   - Detailed error messages

## 📊 Data Transformations

1. **Hex Strings → Bytes**: All hex inputs converted to bytes
2. **Journal → Digest**: SHA-256 hash of journal bytes
3. **Image ID + Journal Digest → Claim Digest**: RISC0 receipt claim
4. **Seal → Proof Points**: Decode G1/G2 elliptic curve points
5. **Journal ABI → Vote Data**: Bincode deserialization
6. **VoteResponse → JSON**: Dataclass to dictionary conversion

## ⚡ Performance Considerations

- **Async Endpoint**: FastAPI async handler for non-blocking I/O
- **Crypto Operations**: CPU-intensive pairing checks
- **In-Memory Storage**: No database queries (albums stored in memory)

